meta {
  name: pull
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/sync/pull?masterKeyVersion=1
  body: none
  auth: bearer
}

params:query {
  masterKeyVersion: 1
}

auth:bearer {
  token: {{sessionToken}}
}
