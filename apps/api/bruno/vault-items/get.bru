meta {
  name: get
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/vault/items/{{itemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{sessionToken}}
}

docs {
  Run vault-items/create first -- it populates {{itemId}}.
}
